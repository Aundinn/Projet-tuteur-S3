{% extends "::layout.html.twig" %}

{% block title %}{{ parent() }} - Accueil{% endblock %}

{% block body %}
<nav>
    <ul id="menuArchive">
        <li><a href=" {{ path('pr2_l_archives_archives') }}">Consulter</a></li>
        <li><a href=" {{ path('pr2_l_archives_rechercheAvancee') }}">Recherche Avancée</a></li>
        <li><a href=" {{ path('pr2_l_archives_ajouter') }}">Ajouter une archive</a></li>
        <li><a href="">Modifier une archive</a></li>
    </ul>
</nav>

    <table>
        <tr><th>Côte de l'archive</th><th>Titre Archive</th><th>Nom du fond d'archive</th><th>Resumé de l'archive</th><th>Classification de l'archive</th><th></th></tr>
        {% for advert in listAdverts %}
        <tr><td>{{ advert.getCote() }}</td>
            <td>{{ advert.getTitreArchive() }}</td>
            <td>{{ advert.getNomDuFond() }}</td>
            <td>{{ advert.getResume() }}</td>
            <td>{{ advert.getClassification() }}</td>
            <td><a href="{{ path('pr2_l_archives_detail',{'id':advert.getId()}) }}">Plus d'informations</a></td></tr>
        {% endfor %}
    </table>

   
    
{% endblock %}
