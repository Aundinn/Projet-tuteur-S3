{% extends "::layout.html.twig" %}

{% block title %}{{ parent() }} - Connexion{% endblock %}

{% block body %}

{% if getLogin is defined %}
    coucou
{% else %}
    <h3>Formulaire de connexion</h3>
    
    <div class="formulaire">
        <form method="post" action="#" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
            <input type="submit"/>
        </form>
    </div>
{% endif %}

{% endblock %}
